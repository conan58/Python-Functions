A function that takes a string as a parameter and returns the number of vowels in the string:

def count_vowels(string):
    vowels = "aeiou"
    count = 0
    for char in string:
        if char.lower() in vowels:
            count += 1
    return count

sentence = "This is a test sentence."
result = count_vowels(sentence)
print(result) # 6
